in settings.py

`pip install psycopg2`

```js
DATABASES = {
  default: {
    ENGINE: "django.db.backends.postgresql",
    NAME: "bingology",
    USER: "postgres",
    PASSWORD: "yilmaz2707",
    HOST: "localhost",
    PORT: "5432",
  },
};
```
